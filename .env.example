# Google OAuth Web application Client ID (public identifier, not a client secret).
# Add the local origin http://localhost:5179 and your production origin in Google Cloud.
VITE_GOOGLE_BLOGGER_CLIENT_ID=
